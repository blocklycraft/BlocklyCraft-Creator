// app global css

@font-face
  font-family Noto
  src url(./fonts/NotoSerifCJKsc-Medium.otf)

html,body {
  font-family 'Roboto', 'Noto';
  overflow:hidden;
}
.bg-background {
  background-color var(--q-color-background);
}
.q-menu {
  background-color var(--q-color-background);
  color var(--q-color-text)
}
